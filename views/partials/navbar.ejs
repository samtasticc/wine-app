<nav class="navbar">
    
    <% if(user) { %>
        <a href="/users/<%= user._id %>/wines">My Sips</a>
        <a href="/users/<%=user._id%>/wines/new">Add a New Sip</a>
        <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
        <a href="/auth/sign-in">Sign In</a>
        <a href="/auth/sign-up">Sign Up</a>
    <% } %>
</nav>